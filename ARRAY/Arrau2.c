#include <stdio.h>

int main() {
  // 1) Declare an int array named numbers with 5 elements

  // 2) Set numbers[0] to 10

  // 3) Set numbers[4] to 50

  // 4) Print numbers[0]
  // 5) Print numbers[4]
int numbers [5]= {10,20,30,40,50};



printf ("%d",numbers[0]);
printf ("%d",numbers[4]);
  return 0;
}
//Also, your filename ends with .C, which some compilers treat as
 // C++. For C code, prefer .c.//